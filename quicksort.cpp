#include <iostream>
#include <vector>
#include "time.h"
#include "stdlib.h"
using namespace std;
int unsorted[];
int sorted[];
void quicksort();
int siz = 0;
int main()
{
    srand(time(NULL));
    
    cout << "Please enter the size of the array of integers" << endl;
    cin >> siz;
    unsorted[siz];

    for (int i = 0; i < siz; i++)
    {
        int numbers = rand() % 100;
        
        cout << v1[i] << endl;
    }
    
    cin.get();
}

void quicksort()
{
    for (int i = 0; i < siz; i++)
    {

    }
}